// Variaveis boleanas só podem ter valores: verdadeiros ou falsos.
// são muito importantes na programação porque vão existir explicitamente.
// ou implicitamente em decisões do código que permitem definir o seu fluxo.

let sim = true;
let não = false;

let resultado = 10 > 5;
console.log(resultado);

// iremos falar mais sobre booleanos á frente.